name: first_flutter_app
description: "A simple test Flutter app with just a start screen."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sensors_plus: ^6.1.1
  proximity_sensor: ^1.0.3
  vibration: ^2.0.1
  flutter_local_notifications: ^18.0.1
  camera: ^0.11.0+2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
